rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS ABIERTAS PARA DESARROLLO - Permitir todo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}